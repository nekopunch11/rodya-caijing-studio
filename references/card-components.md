# 数据卡片层（结构规范，占位版）

> ⚠️ 状态：**视觉细节 defer 中**。本文件只锁"结构硬点 + 字段功能位"，不是可直接出图模板。色彩/字体/间距等视觉 token 待视觉专场与用户敲定后填入，届时**只改本文件与 assets/**，七模块全生效。

## 一、已锁硬点（三条，影响结构，不再变）

1. **竖版 3:4**（微信对话/朋友圈/小红书通吃）。
2. **底部固定件（footer）**，从上到下：数据截至日 + 来源简注 → 固定免责声明（见 compliance.md 第四节）→ slogan 位「客观体检 · 不含买卖建议」。
3. **红涨绿跌**，锁死 A 股惯例（红=上涨/超预期，绿=下跌/不及预期），任何图表不得反用。

## 二、通用布局意图（自上而下）

| 区块 | 内容来源（内核字段） |
|---|---|
| 标题区 | 标的 + 代码 + 模块名（如「基本面体检」） |
| 主视觉区 | 各模块签名图形（见下表） |
| 关键数字区 | `关键数字` 取 3–5 条，大号数字 + 小字对比口径 |
| 风险提示区 | `风险` 取严重度最高 1 条 |
| footer 固定件 | 见硬点 2 |

## 三、各模块签名图形（建议版，⚠️ 未经用户确认，视觉专场敲定）

| 模块 | 卡片类型建议 |
|---|---|
| 基本面 | 体检雷达卡（护城河五源/盈利/质量/成长/风险五维） |
| 财报 | 财报速览卡（同比环比箭头 + 超预期标记） |
| 估值 | 分位 band 图（历史区间 + 当前位置点） |
| 排雷 | 风险提示卡（公开信息提示项 + 重点关注项） |
| 产业链 | 上中下游图谱卡（环节 + 利润池深浅） |
| A股打新 | 打新速览卡（发行 PE 对比 + 板块同类组破发率） |
| 港股打新 | 打新速览卡（关键因子 + 同类组破发率） |

## 四、视觉 token（占位，视觉专场填值）

```css
--color-primary: /* 待定 */;
--color-up: /* 红，待定具体值 */;
--color-down: /* 绿，待定具体值 */;
--color-bg: /* 白/米白，待定 */;
--font-num: /* 等宽/tabular figures，待定字体 */;
--font-text: /* 中文无衬线，待定 */;
```

数字必须用等宽数字（tabular figures），大号关键指标 + 小字来源脚注——这条属于专业感基线，随 token 一起定稿。

## 五、生成管线（机制已定）

复用 HTML → html2canvas → 2× PNG 的管线**机制**（同 skill-fmt 的技术路径），**审美另起**：受众是投顾，要克制的专业感，不用霓虹赛博风。卡片 HTML 模板届时放 `assets/`。
